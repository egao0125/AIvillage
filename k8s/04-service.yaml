apiVersion: v1
kind: Service
metadata:
  name: ai-village
  namespace: ai-village
  labels:
    app: ai-village
spec:
  type: ClusterIP
  selector:
    app: ai-village
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
