apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-village-config
  namespace: ai-village
data:
  PORT: "8080"
  NODE_ENV: "production"
  # Comma-separated list of allowed CORS origins.
  # Example: https://aivillage.example.com,https://www.aivillage.example.com
  ALLOWED_ORIGINS: "https://aivillage.example.com"
  # Maximum number of agents allowed per simulation instance.
  MAX_AGENTS: "20"
  COGNITO_REGION: "ap-northeast-1"
  COGNITO_USER_POOL_ID: "ap-northeast-1_9SlumyaUE"
  COGNITO_CLIENT_ID: "6ap94a8dvl9s01vtvrv58aai7q"
