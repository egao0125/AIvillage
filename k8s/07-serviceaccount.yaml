apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-village
  namespace: ai-village
  annotations:
    # IRSA: this SA assumes the app IAM role (injected by terraform output app_irsa_role_arn)
    # Update this ARN after `terraform output app_irsa_role_arn`
    eks.amazonaws.com/role-arn: "arn:aws:iam::053442321898:role/ai-village-app"
